*.so
*.pyc
*.pyo
*.bin
*_stub.pyi
*_generated.go
*_generated.s
*_generated_test.go
*_generated_test.s
*_generated.h
*.local.md
/dependencies
/tags
/build/
/fonts/
/shaders/
/linux-package/
/kitty.app/
/glad/out/
/kitty/launcher/kitt*
# The Windows port keeps two launcher sources in that directory, and the
# pattern above, which is there for the built launcher, was swallowing them.
!/kitty/launcher/kitty.rc
!/kitty/launcher/kitty.ico
/*.dSYM/
__pycache__/
/glfw/wayland-*-client-protocol.[ch]
/docs/_build/
/docs/generated/
/tools/simdstring/simdstring.test
/.ruff_cache
.DS_Store
.cache
bypy/b
bypy/virtual-machines.conf
_codeql_detected_source_root
*.pyd

# Local Windows-port task list
WINDOWS_TODO.md
dist/
packaging/windows/cert/

# Stray output from "go build ./tools/cmd", which writes the binary into the
# working directory. Anchored to the root so the tools/cmd source tree is not
# caught by it.
/cmd
/cmd.exe
